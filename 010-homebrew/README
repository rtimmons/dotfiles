Things to brew:

    # Preliminaries
    
    brew update
    brew prune
    
    brew missing
    # install anything reported by `brew missing`
    
    brew upgrade
    
    brew doctor
    # fix anything pointed out by the good doctor

Then install all the brews

    while read thebrew; do
        if [[ "$thebrew" =~ '^#' ]]; then
            continue
        fi
        echo brew install "$thebrew"
    done < "$ZSH/brew/brews.txt"

Also don't miss these

    cabal install pandoc

